scrape_configs:
  - job_name: rune
    scrape_interval: 10s
    static_configs:
      # Uses Docker Compose's internal DNS — "rune" resolves to the
      # Rune container. Port 8000 is the container-internal port.
      - targets: ["rune:8000"]
