scrape_configs:
  - job_name: rune
    scrape_interval: 10s
    static_configs:
      # host.docker.internal + extra_hosts (in docker-compose.yml) lets the
      # container reach `rune server` running on the host's port 8000.
      - targets: ["host.docker.internal:8000"]
